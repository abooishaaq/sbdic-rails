<div id="<%= dom_id item %>" class="flex flex-col justify-center items-start max-w-screen w-full my-4">
  <h2 class="text-2xl"><%= item.title %></h2>
  <p class="overflow-auto"><%= item.desc %></p>
  
  <div class="flex">
    <p>count:&nbsp;</p>
    <p><%= item.count %></p>
  </div>

</div>
